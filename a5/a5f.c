// (a5f.c) Measure the speed with which one can write 8192 bytes 
// to the page disk cache. 
#include <stdio.h> 
main() { 
    double seconds=0.0; 
    printf("%e seconds to write 8192 bytes to the disk page cache.\n",seconds); 
} 
